import 'package:flutter/material.dart';

class TextCompletationPage extends StatelessWidget {
  const TextCompletationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Text Completation Screen"),
      ),
    );
  }
}
